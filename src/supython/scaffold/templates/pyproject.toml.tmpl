[project]
name = "{name}"
version = "0.1.0"
description = "A supython project."
requires-python = ">=3.11"
dependencies = [
    "{supython_req}",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["{name}*"]
